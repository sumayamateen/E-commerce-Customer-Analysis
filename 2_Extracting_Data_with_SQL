
-- SQL QUERIES FOR CUSTOMER DATA ANALYSIS
-- This file contains various Google Sheet SQL queries for analyzing customer transaction data

-- 1. Select all invoices and price of items
=QUERY('Customer Data'!A1:H2000, "SELECT E,F",1)

-- 2. Using AND and WHERE to show item description and quantity for prices between $1 and $5
=QUERY('Customer Data'!A1:H2000, "SELECT C, D, F WHERE F >= 1 AND F <= 5", 1)

-- 3. Using OR and WHERE to show item description and quantity for prices between $1 and $5
=QUERY('Customer Data'!A1:H2000, "SELECT C, D, F WHERE F >= 1 OR F <= 5", 1)

-- 4. Top 10 highest-priced items with stock code, description, and price
=QUERY('Customer Data'!A1:H2000, "SELECT B, C, F ORDER BY F DESC LIMIT 10", 1)

-- 5. Invoice number and total price (Quantity * Price) for each line
=QUERY('Customer Data'!A1:H2000, "SELECT A, D * F",1)

-- 6. Total cost for invoice 489447
=QUERY('Customer Data'!A1:H2000, "SELECT A, D * F WHERE A= 489447",1)

-- 7. Average quantity and maximum price
= QUERY('Customer Data'!A1:H2000,"SELECT AVG(D), MAX(F)")

-- 8. Count of invoices, average quantity, sum of price, max and min price
= QUERY('Customer Data'!A1:H2000,"SELECT COUNT(A), AVG(D), SUM(F), MAX(F), MIN(F)")

-- 9. Maximum and minimum InvoiceDate
= QUERY('Customer Data'!A1:H2000,"SELECT MAX(E), MIN(E)")

-- 10. Count of orders by country using GROUP BY
= QUERY('Customer Data'!A1:H2000,"SELECT H, COUNT(H) GROUP BY (H)")
